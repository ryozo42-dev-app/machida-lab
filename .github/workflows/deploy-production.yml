name: Deploy Production

on:
  push:
    branches:
      - main

concurrency:
  group: machida-lab-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: [self-hosted, Linux, X64]

    steps:
      - name: Deploy on VPS
        run: sudo /opt/machida-lab/deploy.sh
